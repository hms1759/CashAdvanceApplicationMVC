@model IEnumerable<MyApplication.Models.MvcDepartment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "AddorEdit")
</p>

        <div class="" style="">

            <h3 class="f" style="font-family:'Times New Roman', Times, serif; font-weight:600">CAsh Advance List</h3>

            <table class="table" style="table-layout:fixed" >
                <tr>
                    <th>
                        <label>
                            Department
                        </label>
                    </th>
                    <th>
                        <label>
                            HOD's Name
                        </label>
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.hodName)
                        </td>
                        <td>
                            <button>
                                @Html.ActionLink("Edit", "Edit", new { id = item.Id })

                            </button> | <button>
                                @Html.ActionLink("Details", "Details", new { id = item.Id })

                            </button>  <button>
                                @Html.ActionLink("Delete", "Delete", new { id = item.Id })

                            </button>
                        </td>
                    </tr>
                }

            </table>
        </div>